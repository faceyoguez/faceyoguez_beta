// Stub for optional modules referenced only in dead AMD/UMD branches of vendor bundles
// (e.g. @zoom/download-manager inside @zoom/meetingsdk's UMD build) that bundlers try
// to statically resolve even though that code path never actually runs.
module.exports = {};
